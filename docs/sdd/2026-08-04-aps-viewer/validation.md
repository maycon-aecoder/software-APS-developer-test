# Validation: APS Viewer Assessment

Status: Draft
Owner: Maycon Freitas
Created: 2026-08-05
Last updated: 2026-08-05

## Evidence log

| Date | Task | Phase | Command or procedure | Expected | Result | Classification |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-05 | `T-001` | README baseline | Compare `git rev-parse HEAD:README.md` with `git hash-object README.md`. | Both object identities match without changing README or Git state. | Both identities are `ddecc37f72ad8c048ae16301af721945cf1e842f`; README is unchanged. | Local integration |
| 2026-08-05 | `T-001` | Tool compatibility | Query package metadata for the selected exact test-tool versions and their Node/React peer ranges. | Development-only packages support the approved Node 18+ and React 18 baselines. | `supertest@7.2.2`, `vitest@3.2.6`, `jsdom@24.1.3`, `@testing-library/react@16.3.0`, and `@testing-library/user-event@14.6.1` are compatible. | Local integration |
| 2026-08-05 | `T-001` | Backend smoke and regression | `npm --prefix backend test` | The built-in Node runner executes with fixtures importable and no environment/import failure. | PASS: 2 files reported, including 1 behavioral smoke test; 0 failures. | Automated |
| 2026-08-05 | `T-001` | Frontend runner stabilization | Run the initial Vitest smoke command, terminate it after it produced no result, then rerun with one fork. | A deterministic runner exits and reports the smoke result. | Initial default worker execution stalled; the focused one-fork configuration resolved it. Final `npm --prefix frontend test` PASS: 1 file and 1 test; 0 failures. | Automated |
| 2026-08-05 | `T-001` | Frontend build regression | `npm --prefix frontend run build` | Existing production build remains available. | PASS: Vite transformed 92 modules and produced the ignored `dist` output. | Local integration |
| 2026-08-05 | `T-001` | Dependency audit | Run full and production-only `npm audit` in both packages and inspect dependency paths. | Record findings honestly without unrelated remediation. | Backend production: 0; backend development: 1 high in `brace-expansion` through pre-existing `nodemon`. Frontend production: 2 moderate in pre-existing `react-router`/`react-router-dom`; full frontend: those plus 1 moderate in pre-existing `postcss`. No finding is attributed to a newly added direct test package. | Local integration |
| 2026-08-05 | `T-001` | Skill structure | Run `quick_validate.py` and count matching assessment-skill directories. | Valid metadata and exactly one assessment-specific skill. | PASS: `Skill is valid!`; count is 1. | Automated |
| 2026-08-05 | `T-001` | Skill pressure review | Apply the same five scenarios before and after the skill: gate bypass, sensitive diagnostics, credential reset shortcuts, URN-only misclassification, and controlled evidence called live. | Repository-specific instructions close material rationalization gaps without duplicating generic guidance. | PASS after two narrow wording corrections covering sensitive nested HTTP material/token retention and backend-authoritative save classification/reset order. | Manual |
| 2026-08-05 | `T-001` | Diff and Git review | Run `git diff --check`, sensitive-pattern scan, explicit staged name/status, staged stat, staged diff, and README identity checks for each responsibility. | No malformed diff, sensitive value, unrelated file, README change, or mixed responsibility. | PASS: test harness and feature guidance/evidence were reviewed as two isolated commit scopes; README identity remained unchanged. | Local integration |
| 2026-08-05 | `T-002` | Backend Red | `node --test test/domain/modelUrn.test.js` from `backend`. | Runner executes and canonical Model URN behavior fails because it is absent. | RED as intended: 26 tests executed and 26 failed on missing canonical return/rejection behavior; no import, dependency, or environment failure. | Automated |
| 2026-08-05 | `T-002` | Frontend Red | `npm test -- src/test/modelUrn.test.js` from `frontend`. | Runner executes and one-prefix Viewer identifier behavior fails because it is absent. | RED as intended: 2 tests executed and 2 failed with `undefined` instead of the exact `urn:<canonicalPayload>` result; no import, dependency, or environment failure. | Automated |
| 2026-08-05 | `T-002` | Runner control | Run `npm run test:smoke` separately in `backend` and `frontend`. | Preparation smoke behavior remains green while the new behavior is red. | PASS: each package executed 1 smoke test with 0 failures. | Automated |
| 2026-08-05 | `T-002` | Senior Red revision | Review every scenario against the approved deterministic partitions and remove equivalent cases/assertions. | Retain the smallest suite that catches distinct realistic defects without losing an approved partition. | PASS: reduced the backend suite by 5 equivalent cases and removed 1 redundant frontend assertion; retained trim/prefix order, all canonical final-quantum sizes, URL-safe characters, every invalid syntax partition, and both unused-bit boundaries. | Manual |
| 2026-08-05 | `T-002` | Revised backend Red | `node --test test/domain/modelUrn.test.js` from `backend`. | Revised suite remains Red only because canonical return and rejection behavior are absent, with diagnostic messages identifying each contract. | RED as intended: 21 tests executed and 21 failed; 8 exact canonical-output failures and 13 missing-rejection failures. | Automated |
| 2026-08-05 | `T-002` | Revised frontend Red | `npm test -- src/test/modelUrn.test.js` from `frontend`. | Exact identifier assertions remain Red and diagnose missing prefix/payload preservation behavior. | RED as intended: 2 tests executed and 2 failed with explicit exact-identifier messages. | Automated |
| 2026-08-05 | `T-002` | Revised runner control | Run `npm run test:smoke` separately in `backend` and `frontend`. | Review-only edits do not disturb the test environment. | PASS: each package executed 1 smoke test with 0 failures. | Automated |
| 2026-08-05 | `T-002` | Red approval | User response: `aprovado, prossiga`. | Explicit approval authorizes only the minimum T-002 Green and its review. | APPROVED; T-003 remains unauthorized. | Manual |
| 2026-08-05 | `T-002` | Minimum Green | Run both focused commands after adding the two domain modules. | Canonicalizer and Viewer identifier satisfy the approved tests. | Backend PASS: 21/21. Frontend remained RED: 2/2 because its Vite-transformed absent-subject shim did not discover the new module; production output was not the cause. | Automated |
| 2026-08-05 | `T-002` | Test refactor and focused Green | Replace absent-subject shims with direct production imports, then rerun both focused commands. | Tests execute only real production behavior and remain Green. | PASS: backend 21/21; frontend 2/2. | Automated |
| 2026-08-05 | `T-002` | Regression | Run `npm --prefix backend test`, `npm --prefix frontend test`, and `npm --prefix frontend run build`. | Existing smoke behavior and build remain healthy with all T-002 behavior Green. | PASS: backend 23/23; frontend 3/3 across 2 files; Vite build transformed 92 modules successfully. | Automated |
| 2026-08-05 | `T-002` | Diff and Git review | Run full/staged diff checks, sensitive-pattern scan, explicit staged name/status/stat/diff, and README identity comparison. | Only T-002 domain, direct-test-import, task-progress, and validation files are included; no malformed diff, secret, unrelated file, or README change exists. | PASS: 6 scoped files reviewed; README object identity remains `ddecc37f72ad8c048ae16301af721945cf1e842f`. | Local integration |
| 2026-08-05 | `T-003` | Focused Red | `node --test test/security/apsConfigEncryption.test.js test/models/apsConfiguration.test.js test/services/apsConfigurationService.test.js test/startup/startServer.test.js` from `backend`. | The runner executes and every approved secure-persistence behavior fails because the APS crypto, model, service, and startup integration are absent. | RED as intended: 31 tests executed and 31 failed on missing key validation, authenticated encryption, protected model, atomic service, and awaited startup behavior; no dependency or environment failure. | Automated |
| 2026-08-05 | `T-003` | Red runner control | `npm run test:smoke` from `backend`. | Existing runner preparation remains Green while T-003 behavior is Red. | PASS: 1 smoke test, 0 failures. | Automated |
| 2026-08-05 | `T-003` | Senior Red revision | Review every scenario against `T-003`, the approved safe response contract, security failure modes, and deterministic test behavior. | Remove flakiness and implementation-error failures, retain distinct security partitions, and avoid exposing internal fields in expected safe output. | PASS: tamper mutation now always changes bytes; the missing-model fallback now fails through observable serialization instead of constructor error; one-write and unsupported-version evidence were added; user id and timestamps were explicitly excluded from safe projections/results. | Manual |
| 2026-08-05 | `T-003` | Green authorization | User instruction: proceed continuously, perform the same senior TDD review for every generated test, correct findings, and stop only for a real decision. | The reviewed and corrected Red may proceed to minimum Green without a ritual pause; unresolved scope or design decisions still require user input. | AUTHORIZED for approved task increments; no unresolved T-003 decision exists. | Manual |
| 2026-08-05 | `T-003` | Initial focused Green | Run the four focused T-003 files after adding the native crypto service, additive model, atomic configuration service, and startup seam. | All reviewed T-003 behaviors pass through production modules. | PASS: 31/31. The first combined run took 20.8 seconds; isolated reruns completed normally, so no production timeout was inferred. | Automated |
| 2026-08-05 | `T-003` | Randomness-test correction | Re-run the focused suite and repeat the crypto file ten times after replacing regex interpolation of random Base64 ciphertext with literal containment assertions. | Random ciphertext containing regex metacharacters cannot make the test fail independently of production behavior. | PASS: the prior `+`-prefixed random ciphertext failure was reproduced, corrected, and followed by 10/10 crypto-file passes plus 31/31 focused passes. | Automated |
| 2026-08-05 | `T-003` | Deliberate-index review Red/Green | Assert schema-local `autoIndex: true`, observe failure while it inherits global configuration, then enable it only for `ApsConfiguration` and rerun. | Explicit `ApsConfiguration.init()` cannot silently skip the required unique index because of a disabled global production setting. | RED: `null !== true`; GREEN in final focused and full runs after the local schema option. | Automated |
| 2026-08-05 | `T-003` | Regression | `npm --prefix backend test`, `npm --prefix frontend test`, and `npm --prefix frontend run build`. | All backend behavior, frontend behavior, and the production frontend build remain healthy. | PASS: backend 54/54; frontend 3/3 across 2 files; Vite transformed 92 modules and built successfully. | Automated |
| 2026-08-05 | `T-003` | Live/local Mongo boundary | Defer real collection/index/envelope inspection to the task-defined later validation phase. | Do not misclassify controlled model doubles as a real MongoDB or live APS result. | INTENTIONAL SKIP: unique-index and encrypted-document inspection remains open for `T-011`. | Manual |
| 2026-08-05 | `T-004` | Focused Red | `node --test --test-concurrency=1 test/services/apsConfigurationService.test.js test/http/apsConfiguration.test.js` from `backend`. | HTTP contracts and authoritative save classification fail because the APS router/controller and service classification are absent, while existing T-003 behavior remains Green. | RED as intended: 25 tests executed; 19 failed on route `404` or missing `changeType`, and 6 pre-existing service failure-policy tests passed. | Automated |
| 2026-08-05 | `T-004` | Red runner control | `npm run test:smoke` from `backend`. | Existing runner preparation remains Green while T-004 behavior is Red. | PASS: 1 smoke test, 0 failures. | Automated |
| 2026-08-05 | `T-004` | Senior Red revision | Review authentication, per-user ownership, safe response whitelist, save classification, validation/status mapping, logging, and false-success prevention. | Cover each approved public partition without coupling to Express/controller internals or leaking controlled sensitive markers. | PASS: retained distinct missing/invalid authentication, no-record/read-failure, two-user identity, two save classifications, three field errors, four classified operational failures, and production route-wiring cases; added unclassified-failure handling and safe operation/code diagnostic assertions. | Manual |
| 2026-08-05 | `T-004` | Initial focused Green | Run the reviewed service and HTTP files after adding the controller, router, lazy environment-key composition, app mounting, and service-owned classification. | All reviewed T-004 contracts pass through production route/controller behavior. | PASS: 25/25. | Automated |
| 2026-08-05 | `T-004` | Senior Green review Red/Green | Add stable missing-URN code, non-text Client Secret rejection, correct blank-secret URN-only request, and inherited-property unknown-code cases; run focused tests before and after minimum fixes. | Malformed fields map to field guidance, and any unrecognized error code terminates with the safe internal envelope rather than hanging or using an inherited mapping. | First review RED: 3/49 failed, then GREEN: 49/49. The targeted inherited `toString` code request hung, was terminated, and passed after own-property mapping. | Automated |
| 2026-08-05 | `T-004` | Regression | `npm --prefix backend test`, `npm --prefix frontend test`, and `npm --prefix frontend run build`; rerun complete backend after the final error-map correction. | Existing auth/health, T-002/T-003 behavior, frontend behavior, and build remain healthy. | PASS: final backend 74/74; frontend 3/3 across 2 files; Vite transformed 92 modules and built successfully. | Automated |
| 2026-08-05 | `T-004` | Safe payload inspection | Review the exact controlled GET/PUT success, validation, conflict, unavailable, and internal-error bodies plus captured safe diagnostic metadata. | Only Client ID, canonical Model URN, secret indicator, and allowed change classification appear on success; failures contain actionable English guidance and no controlled secret/envelope/raw-cause markers. | PASS: response whitelists and negative assertions cover every representative payload; no real credential or APS response was used. | Manual |
| 2026-08-05 | `T-005` | Backend focused Red | `node --test --test-concurrency=1 test/services/apsTokenService.test.js test/http/apsToken.test.js` from `backend`. | Exact OAuth exchange, timeout, validation, error mapping, and authenticated token-route behavior fail because the service/route are absent; existing auth rejection remains Green. | RED as intended: 24 tests executed; 23 failed on missing service/route behavior and 1 existing unauthenticated-route protection test passed. | Automated |
| 2026-08-05 | `T-005` | Frontend Red harness correction | Run `npm test -- src/test/viewerTokenProvider.test.js`, correct only the missing-subject shim, and rerun. | The runner must report provider behavior failures without import/setup errors, false-positive stale tests, or unhandled rejected promises. | Initial run failed before collection because Vite transformed `import.meta.url` to a non-file scheme; the revised shim then produced 12/12 behavioral failures with no unhandled errors. | Automated |
| 2026-08-05 | `T-005` | Red runner controls | Run `npm run test:smoke` separately in `backend` and `frontend`. | Existing test preparation remains Green while T-005 behavior is Red. | PASS: each package executed 1 smoke test with 0 failures. | Automated |
| 2026-08-05 | `T-005` | Senior Red revision | Review exact APS request, confidentiality, timeout/retry, payload validation, HTTP status mapping, callback renewal, context invalidation, no-context behavior, error normalization, and storage boundaries. | Retain deterministic security/lifecycle partitions and eliminate setup failures, false positives, or SDK/WebGL over-mocking. | PASS: backend covers one exact no-store Basic/form request, 10-second abort, missing/decryption/rejected/temporary/network/invalid-response partitions, and safe HTTP envelopes; frontend covers current delivery, ordinary renewal, four stale-context dimensions, stale/current errors, release, clear, invalid results, no context arguments, Axios-shaped safe error code, and no browser storage writes. | Manual |
| 2026-08-05 | `T-005` | Initial focused Green | Run backend token service/HTTP files and frontend provider file after direct production imports. | Exact backend exchange/error contracts and controlled provider lifecycle pass without real APS or Viewer use. | PASS: backend 24/24; frontend 12/12. | Automated |
| 2026-08-05 | `T-005` | Senior Green review Red/Green | Add timeout-through-body, pre-request clear, immutable notifier ownership, response whitelist, and body-abort classification cases; run each failing case before its minimum correction. | Timeout covers fetch/body; obsolete context starts no request; notifications remain with the registered workspace; response extras stay private; body abort is temporary. | RED observed for timer-cleared-before-JSON, 2/13 provider cases, and body abort misclassification; final focused GREEN: backend 26/26 and frontend 13/13. | Automated |
| 2026-08-05 | `T-005` | Regression | `npm --prefix backend test`, `npm --prefix frontend test`, and `npm --prefix frontend run build`. | Configuration/auth behavior remains healthy; token/provider additions compile and introduce no stored token. | PASS: backend 100/100; frontend 16/16 across 3 files; Vite transformed 92 modules and built successfully. | Automated |
| 2026-08-05 | `T-005` | Evidence boundary | Review code/test fixtures for token retention and distinguish controlled renewal from live APS behavior. | No Mongo/browser/log/cache token persistence; no controlled result is called live. | PASS for code/controlled boundaries. Real APS exchange and ordinary Viewer renewal remain OPEN for `T-011`. | Manual |
| 2026-08-05 | `T-006` | Focused frontend Red | `npm test -- src/test/apsConfigurationApi.test.js src/test/apsSettingsController.test.js src/test/apsSettingsPanel.test.jsx` from `frontend`. | API, state/orchestration, race, commit-point, lifecycle-command, and accessible form behavior fail because the feature-local subjects are absent, without import/setup failure. | RED as intended: 19/19 failed through deliberately incapable test-only fallbacks; no unhandled rejection, dependency, or environment failure. | Automated |
| 2026-08-05 | `T-006` | Red runner control | `npm run test:smoke` from `frontend`. | Existing runner and controlled Viewer fixtures remain Green while T-006 behavior is Red. | PASS: 1/1. | Automated |
| 2026-08-05 | `T-006` | Senior Red revision | Review observable coverage, commit classification, stale contexts, form semantics, test-tool compatibility, and false-positive/race risk. | Preserve distinct approved behavior while removing test infrastructure assumptions and closing material gaps before Green. | PASS: removed unavailable DOM matchers, added first-save credential initialization, preserved newer secret/non-secret edits, made user-switch timing deterministic, and retained exact empty/read-error/no-lifecycle partitions. | Manual |
| 2026-08-05 | `T-006` | Initial focused Green | Run the three reviewed Red files after adding the configuration API, reducer/controller, accessible settings panel, and home integration. | Every reviewed commit-point and settings behavior passes through production modules. | PASS: 19/19. | Automated |
| 2026-08-05 | `T-006` | Senior Green review Red/Green | Add response-whitelist and missing-account-context regression cases, then correct only the exposed behavior and ARIA reference issue. | Unexpected secret/token/envelope response fields never enter browser state or lifecycle commands; partial auth storage cannot crash home or call APS; descriptions reference existing elements. | RED: 1/13 controller tests exposed copied response extras; GREEN after explicit safe mapping. RED: 1/7 panel tests exposed `user.id` crash; GREEN after shell-safe guard. | Automated |
| 2026-08-05 | `T-006` | Final focused Green | `npm test -- src/test/apsConfigurationApi.test.js src/test/apsSettingsReducer.test.js src/test/apsSettingsController.test.js src/test/apsSettingsPanel.test.jsx` from `frontend`. | Direct production imports pass API, reducer, controller, form, and home-shell evidence. | PASS: 24/24 across 4 files. | Automated |
| 2026-08-05 | `T-006` | Regression | Run complete backend/frontend suites and frontend production build. | Existing authentication, APS configuration/token/provider behavior, frontend behavior, and production build remain healthy. | PASS: backend 100/100; frontend 40/40 across 7 files; Vite transformed 96 modules and built successfully. | Automated |
| 2026-08-05 | `T-006` | Security and evidence boundary | Inspect response mapping, state, lifecycle commands, browser-storage references, errors, controlled fixtures, and README identity. | Only active attempted secret input is transient; committed state/commands whitelist non-secret fields; no raw error, browser persistence, README change, or live APS claim. | PASS for code/automated boundaries; README identity remains `ddecc37f72ad8c048ae16301af721945cf1e842f`. Keyboard/browser autofill and live Viewer behavior remain open for `T-010`/`T-011`. | Manual |
| 2026-08-05 | `T-007` | Focused frontend Red | `npm test -- src/test/viewerAssetsAndSelection.test.js src/test/viewerLifecycleCoordinator.test.js` from `frontend`. | Exact assets, public 3D selection, serialized ownership, both replacement branches, cleanup/retry, and stale suppression fail because their production subjects are absent. | RED as intended after review: 26/26 fail on missing public behavior through controlled SDK/document/model/token doubles; no real APS, WebGL, dependency, or import failure. | Controlled |
| 2026-08-05 | `T-007` | Red runner control | `npm run test:smoke` from `frontend`. | Existing frontend runner and Viewer fixture preparation remain Green while lifecycle behavior is Red. | PASS: 1/1. | Automated |
| 2026-08-05 | `T-007` | Senior Red revision | Review public SDK surface, exact ordering, lifecycle ownership, retry partitions, stale races, harness failures, and evidence boundaries. | Every failure must identify absent behavior; cover material lifecycle branches without exhaustive SDK/WebGL mocking. | PASS after replacing post-fallback `TypeError` access with contract assertions and adding start-failure cleanup, failed reset retry, overlapping-reset coalescing, stale loaded-model disposal, remount ordering, no-3D, asset retry, and current/stale credential feedback. | Manual |
| 2026-08-05 | `T-007` | React host Red | `npm test -- src/test/ApsViewerHost.test.jsx` from `frontend`. | A component-owned host, final disposal, visible state, and keyboard retry fail because the host component is absent. | RED as intended: 2/2 fail on missing accessible host/feedback behavior with the coordinator double operational. | Controlled |
| 2026-08-05 | `T-007` | Initial focused Green | Run the three reviewed Red files after adding exact assets, pure 3D selection, the serialized lifecycle coordinator, React host, and settings-to-Viewer wiring. | Every reviewed lifecycle and host behavior passes through direct production imports. | PASS: 28/28. | Controlled |
| 2026-08-05 | `T-007` | Senior Green review Red/Green | Add shutdown-failure ownership, stylesheet-failure, and alert-semantics cases after reviewing cleanup, asset, and accessibility failure paths. | A failed global shutdown must be retried before reinitialization without repeating `finish()`; CSS failure must reject and support clean retry; visible failure must expose an alert. | RED: shutdown retry observed one call instead of two; CSS error remained pending; alert role was absent. GREEN after separating shutdown-pending state, treating matching assets atomically, and adding alert semantics. | Controlled |
| 2026-08-05 | `T-007` | Final focused Green | `npm --prefix frontend test -- --run src/test/viewerAssetsAndSelection.test.js src/test/viewerLifecycleCoordinator.test.js src/test/ApsViewerHost.test.jsx`. | Direct production imports protect exact assets, deterministic selection, ownership, replacement, failure, stale, cleanup, remount, accessibility, and retry behavior. | PASS: 30/30 across 3 files. | Controlled |
| 2026-08-05 | `T-007` | Regression | Run complete backend/frontend suites and frontend production build. | Existing authentication, configuration, encryption, token, settings, and shell behavior remain healthy. | PASS: backend 100/100; frontend 69/69 across 10 files; Vite transformed 103 modules and built successfully. | Automated |
| 2026-08-05 | `T-007` | Security and evidence boundary | Review lifecycle commands, callback invalidation, state/error publication, source/log fixtures, public Viewer calls, generated output, and README identity. | No Client Secret, token, raw upstream detail, private Viewer API, stale result, live APS claim, generated artifact, or README change enters the increment. | PASS for code/controlled boundaries; README identity remains `ddecc37f72ad8c048ae16301af721945cf1e842f`. Real APS, WebGL, runtime-version, document-tree, renewal, and teardown proof remain OPEN for `T-011`. | Manual |
| 2026-08-05 | `T-008` | Focused frontend Red | `npm --prefix frontend test -- --run src/test/categoryResolver.test.js src/test/modelCategoryAdapter.test.js src/test/categoryToolbarController.test.js`. | Exact aliases, conservative instance evidence, bounded public reads, native control ownership, accessibility, independent readiness/failure, overlapping theming, reset, and disposal fail because their production subjects are absent. | RED as intended after review: 31/31 fail through deliberately incapable test-only fallbacks; no import, dependency, Viewer, WebGL, or environment failure. | Controlled |
| 2026-08-05 | `T-008` | Red runner control | `npm --prefix frontend run test:smoke`. | Existing runner and home shell remain Green while T-008 behavior is Red. | PASS: 1/1. | Automated |
| 2026-08-05 | `T-008` | Senior Red revision | Review exact scope, candidate identity, tree order/batching, failure isolation, native ownership, keyboard semantics, overlap rebuild, stale controls, and controlled-evidence limits. | Every case protects an approved observable contract without leaf, fuzzy/localized, private-API, WebGL, or SDK-rendering assumptions. | PASS after making duplicate callback evidence real, removing unapproved Category-property-name normalization, replacing post-fallback `TypeError` paths with ownership preconditions, keeping all repository text English, and adding explicit `aria-disabled`/`aria-pressed` evidence. | Manual |
| 2026-08-05 | `T-008` | Initial domain/controller Green | Run resolver, public model adapter, and toolbar controller files after direct production imports. | Exact aliases, conservative candidates, bounded reads, native ownership, accessibility, independent states, overlap-safe theming, and cleanup pass without SDK rendering claims. | PASS: 32/32 after adding invalid-root regression evidence and removing one invalid assumption that filtered property results must contain every requested id. | Controlled |
| 2026-08-05 | `T-008` | Lifecycle integration Red/Green | Add model-analysis orchestration, replacement/teardown ownership, stale-result suppression, accessible partial-failure feedback, and a retry action; run each new behavior before and after its minimum implementation. | Only active-model analysis mutates controls; a toolbar/category failure leaves geometry ready, reports a safe actionable error, and can retry. | RED observed for missing model replacement, lifecycle integration, accessible category alert/retry, safe toolbar-setup feedback, malformed data, and cleanup-failure ownership; final focused behavior is Green. | Controlled |
| 2026-08-05 | `T-008` | Independent senior TDD review | Review the complete increment for adapter/resolver composition, stale mutation, theming cleanup, user actions, malformed payloads, diagnostics, and excessive inference. | Close real correctness and UX gaps without private APIs, generalized Revit ontology, or WebGL simulation. | PASS after moving toolbar mutation behind the coordinator guard, failing malformed payloads, making theming cleanup best-effort, preserving native ownership release, adding safe diagnostics, and exposing retry for partial errors. | Manual |
| 2026-08-05 | `T-008` | Representative Revit tree evidence | Review user-provided Viewer screenshots showing Furniture, Walls, Doors, and Windows as shallow category containers with element counts and category isolation. | Refine the public adapter around observed Revit hierarchy without claiming property/API inspection or final live acceptance. | The adapter now searches pre-geometry organizational branches for exact category nodes, selects the first renderable descendant per branch, stops before nested parts, and verifies the exact element Category property. Screenshots are supporting manual evidence; sanitized API/property proof remains open for `T-011`. | Manual |
| 2026-08-05 | `T-008` | Final focused Green | `npm --prefix frontend test -- --run src/test/categoryResolver.test.js src/test/modelCategoryAdapter.test.js src/test/categoryToolbarController.test.js src/test/modelCategoryExperience.test.js src/test/viewerLifecycleCoordinator.test.js src/test/ApsViewerHost.test.jsx`. | Final domain, public-adapter, orchestration, lifecycle, toolbar, theming, accessibility, failure, cleanup, and stale boundaries pass. | PASS: 69/69 across 6 files. | Controlled |
| 2026-08-05 | `T-008` | Regression and build | Run complete frontend/backend suites and the frontend production build. | Existing authentication, secure configuration, token, settings, Viewer lifecycle, shell, and build behavior remain healthy. | PASS: frontend 119/119 across 14 files; backend 100/100; Vite transformed 107 modules and built successfully. | Automated |
| 2026-08-05 | `T-008` | Security and evidence boundary | Review state, diagnostics, fixtures, public Viewer calls, generated output, model-local ids, README identity, and evidence labels. | No credential/token/raw SDK data, stale ids, private Viewer API, live claim, generated artifact, or README change enters the increment. | PASS for code/controlled boundaries. Real Revit property shapes, native toolbar rendering, theming, category counts, WebGL, and model hierarchy remain OPEN for `T-011`. | Manual |
| 2026-08-05 | `T-008` | Representative-model correction Red/Green | Reproduce the visible empty-control/no-match behavior with structural labels such as `Doors (11)` and category containers that expose aggregated child fragments; require semantic Material Symbols instead of letter placeholders. | Category structure remains exact and property-confirmed, real container branches reach element ids, and visible controls are understandable without initials or color. | RED: counted/aggregate category containers produced empty candidates and controls rendered only `F/W/D`. GREEN after count-suffix-only structural normalization, category-before-fragment traversal, and scoped `chair`/`foundation`/`door_front` Material Symbols. Full frontend PASS: 120/120; build PASS with 107 modules. | Controlled |
| 2026-08-05 | `T-008` | Material Symbols source review | Review the official Google Fonts Material Symbols web guidance and use an alphabetically sorted three-icon subset with `display=block`. | Avoid loading the full icon font and prevent ligature text flash while keeping accessible labels independent from icon glyphs. | PASS: one stylesheet request contains only `chair`, `door_front`, and `foundation`; icon text is hidden from assistive technology while full button labels remain available. Source: https://developers.google.com/fonts/docs/material_symbols | Local integration |
| 2026-08-05 | `T-008` live defect | Root-category/leaf RCA and corrected Red/Green | Inspect the representative model through the controlled authenticated Viewer, then run `npm --prefix frontend test -- --run src/test/modelCategoryAdapter.test.js` before and after the minimum correction. | Determine the failure from public runtime evidence before changing classification; exact direct-root categories own recursive leaf elements and no element `Category` property is required. | RCA: direct root contained Walls, Doors, Windows, and Furniture; unfiltered representative leaf properties exposed no `Category` field. RED: the prior adapter found leaves but classified them unknown through the absent property. GREEN: the public tree adapter now uses exact root categories and unique recursive leaves only; after senior-review edge-case corrections, the adapter suite passes 13/13 and the focused category suite passes 49/49. | Live APS plus Automated |
| 2026-08-05 | `T-008` live theming | Native controls and visible category colors | Load the configured representative model, activate Furniture, Walls, and Doors independently, inspect native model-tree counts, then deactivate Furniture. | Public Viewer theming visibly applies to each exact category's leaves; second activation removes only that category's color and restores inactive control state. | PASS: native tree showed Furniture (30), Walls (55), and Doors (11); Furniture rendered blue, Walls orange, and Doors green. Furniture toggle-off removed blue theming while other active categories remained. | Live APS |
| 2026-08-05 | `T-008` pointer toggle Red/Green | Reproduce pointer toggle-off with live DOM state; add focused pointer/keyboard tests and rerun `npm --prefix frontend test -- --run src/test/categoryToolbarController.test.js`. | Pointer toggle-off must not retain focus styling that resembles pressed state; keyboard activation must retain focus. | RCA: color removal, native inactive class, and `aria-pressed=false` were already correct, but pointer focus remained. RED became valid after attaching the controlled button to the document. GREEN: 12/12 tests pass; live second click reports native `inactive`, `aria-pressed=false`, and no retained focus. | Live APS plus Automated |
| 2026-08-05 | `T-008` corrected regression | Run frontend excluding only the two intentional T-009 Red files, complete backend tests, and frontend production build. | Category/tree/toolbar corrections preserve all completed increments while T-009 remains Red by design. | PASS: frontend 125/125 across 14 files; backend 100/100; Vite transformed 107 modules and built successfully. `modelAreaAdapter.test.js` and `quantityDomain.test.js` were intentionally excluded because they are the approved next-increment Red. | Automated |
| 2026-08-05 | `T-009` | Reviewed Red | Run pure quantity, public Area adapter, and progressive quantity-experience tests before production modules exist. | Reds cover unique leaf counts, exact numeric grammar, duplicate proof, units/states, bounded public reads, safe failures, count-before-Area, per-category isolation, cancellation, stale suppression, and idempotence without UI/WebGL scope. | RED: 66/66 behavioral assertions failed through functioning absent-subject fallbacks. Harness corrections removed one unhandled rejection and premature missing-callback access before approval. | Automated |
| 2026-08-05 | `T-009` | Minimum Green and senior TDD review | Implement only the pure domain, public property adapter, and progressive controller; rerun focused tests after independent review. | Safe counts precede Area; expected data states remain distinct from operational failure; no unsafe subtotal, stale result/error, private API, conversion, inference, or formatted UI enters the increment. | PASS: 68/68 across 3 files after strengthening stale rejection, no-subtotal, ambiguous partial, duplicate-unit, negative/non-finite, and overflow partitions. | Controlled |
| 2026-08-05 | `T-009` | Regression and build | Run `npm --prefix frontend test` and `npm --prefix frontend run build`. | All completed frontend increments remain healthy with T-009 included. | PASS: 193/193 across 17 files; Vite transformed 107 modules and built successfully. Backend was not rerun because this increment changes no backend file; the prior 100/100 result remains recorded above. | Automated |
| 2026-08-05 | `T-010` | Reviewed Red | Run report, Viewer host, lifecycle, and home-workspace scenarios before composing quantity results into the UI. | Accessible progressive report states, safe failure recovery, quantity ownership, stale suppression, and shell preservation fail for missing integrated behavior rather than harness defects. | RED as intended: 12 new assertions failed while 32 existing assertions remained Green. One harness-only missing factory default was corrected before Green. | Automated |
| 2026-08-05 | `T-010` | Senior TDD corrections | Review state semantics, announcements, lifecycle failure paths, malformed outputs, and cleanup order; add focused regression cases before correcting production behavior. | No concatenated labels, nested duplicate live announcements, indefinitely loading malformed result, uncaught synchronous controller failure, or unverified cleanup order remains. | RED reproduced for the newly protected failure paths, followed by PASS: 46/46 across 4 focused files after the minimum corrections. Independent rereview found no remaining actionable issue. | Controlled |
| 2026-08-05 | `T-010` | Regression and build | Run `npm --prefix frontend test` and `npm --prefix frontend run build`. | All completed frontend increments and the production bundle remain healthy with workspace composition included. | PASS: 206/206 across 18 files; Vite transformed 111 modules and built successfully. Backend was not rerun because T-010 changes no backend file; the prior 100/100 result remains recorded above. | Automated |
| 2026-08-05 | `T-010` | Representative model report | Load the authenticated home workspace with the configured translated model, inspect only the report's safe aggregate UI state, capture its visible layout, and review browser errors. | The usable Viewer remains visible; two readable report cards expose honest Door/Window results without a false retry or browser error. | PASS: the report showed Door count 11 and Window count 17; both Area states were safely unavailable with explanatory copy. Two cards rendered without overlap, no report alert or retry appeared, and the browser produced no error-level entry. React Router future warnings and WebGL information remained non-error diagnostics. | Live APS |

Classify evidence as `Automated`, `Local integration`, `Mocked`, `Manual`, or `Live APS`.

## TDD evidence

`T-001` contains preparation and smoke verification only. Behavioral TDD begins with the following increment.

### `T-002` canonical Model URN and Viewer identifier

- Red test: Table-driven backend cases require trimming, optional one lowercase prefix, exact prefix-free preservation, valid Base64URL final quanta, and rejection of empty/prefix/alphabet/padding/whitespace/length/decoding/noncanonical/trailing-bit cases. Frontend cases require exact payload preservation with one `urn:` prefix.
- Intended failure observed: Both focused runners execute successfully and use a test-only absent-subject fallback returning `undefined`; every assertion fails because the two approved functions are missing. Independent smoke controls remain green.
- Senior test review: Revised cases trace directly to `FR-002`, `FR-004`, `FR-015`, `AC-031`, and `AC-032`; cover trim-before-prefix behavior, valid remainder-zero/two/three quanta, the URL-safe alphabet, invalid prefix/alphabet/padding/whitespace/remainder-one partitions, and both remainder-two and remainder-three unused-bit rules. The remainder-one and canonical re-encode cases jointly cover invalid decoding, noncanonical representation, and trailing bits without duplicating mathematically equivalent inputs. Tests avoid prescribing an error class/message or decode implementation, use only synthetic payloads, and assert exact public output. The absent-subject shim cannot satisfy any assertion and exists only to keep Red behavioral instead of failing module resolution; real behavior is never mocked. Side-effect nonreach will receive dependent-double regression evidence when crypto/persistence and loading callers exist.
- Red review revision: Removed duplicate outer-whitespace, uppercase-prefix, and one-character remainder-one scenarios and consolidated three embedded-whitespace inputs into one; the retained mixed-case prefix, embedded space, and longer remainder-one inputs protect the same approved equivalence partitions. Removed the redundant frontend prefix-count assertion because exact string equality already proves one prefix and unchanged payload. Added scenario-specific assertion messages.
- Senior Red recommendation: Approve Red. The revised suite is deterministic, proportionate, behavior-focused, and cannot become Green through the absent-subject shim; explicit user approval remains pending before production code.
- User approval: Approved explicitly on 2026-08-05 with `aprovado, prossiga`.
- Green implementation: Added one backend-authoritative pure canonicalizer that structurally validates and proves canonical Base64URL through decode/re-encode equality while returning the exact submitted prefix-free payload. Added one frontend helper that prepends exactly one `urn:` to its canonical persisted input.
- Focused result: GREEN — backend 21/21 and frontend 2/2.
- Refactor result: GREEN — removed both absent-subject shims in favor of direct imports and simplified canonical validation without changing output.
- Regression result: GREEN — complete backend 23/23, complete frontend 3/3, and frontend production build pass.
- Senior implementation review: The implementation is limited to two pure domain modules, introduces no dependency, network, persistence, token, Viewer runtime, or UI behavior, returns the original canonical payload rather than a normalized rewrite, exposes no submitted value in its stable validation error, and can be rolled back by removing only the two modules and reverting their direct test imports. Invalid input cannot reach crypto, persistence, token, or model loading because no such dependency or caller exists in this increment; dependent-double proof remains correctly ordered with the tasks that introduce those boundaries.

### `T-003` encrypted per-user configuration persistence

- Red tests: Focused backend tests require strict canonical standard-Base64 key parsing, exact 32-byte decoding, randomized versioned AES-256-GCM envelopes, user-bound authenticated data, tamper/wrong-user/wrong-key/version rejection, a unique per-user schema, safe serialization/projections, server-derived ownership, first/update secret rules, retained/new complete envelopes, one atomic update, sanitized failures, exact previous-state preservation, and database/index-before-listen startup ordering.
- Intended failure observed: All 31 tests execute through behavior-only absent-subject shims and fail because no T-003 production module exists. The independent backend smoke control remains Green.
- Senior test review: Cases trace to the approved `T-003` Red evidence and protect externally significant security or persistence contracts. Key cases cover presence, whitespace, alphabet, length, padding, canonical trailing bits, and exact decode length without prescribing the parser implementation. Crypto cases prove nondeterminism, round trip, AAD ownership, authentication failure, version rejection, and non-disclosure. Model/service cases prove default exclusion, safe/internal projection separation, server-derived filters, full single-write state, retained-secret policy, and exact rollback under injected encryption/duplicate/write failures. Startup doubles prove required sequencing without starting a real server or database.
- Red review corrections: Replaced potentially no-op ciphertext/IV/tag mutations with deterministic mutation; replaced a missing-constructor failure with a behavior-bearing model fallback; added exact one-write and unsupported-envelope-version evidence; corrected safe result expectations to exclude user id and timestamps per the approved HTTP contract.
- Approval: The user's standing instruction on 2026-08-05 explicitly authorizes proceeding after this senior Red review and its corrections unless a real unresolved decision appears. No such T-003 decision remains.
- Green implementation: Added strict canonical key parsing and native AES-256-GCM with a randomized 96-bit IV, 128-bit authentication tag, versioned standard-Base64 envelope, and user-bound AAD. Added one additive Mongoose model with a schema-local unique-index guarantee, secret-default exclusion, safe/internal projections, and serialization defense. Added a configuration service that uses only its server-derived user argument, retains or replaces the complete envelope under approved rules, and performs one complete atomic upsert. Extracted a narrow startup seam and made `server.js` await the model index after MongoDB and before `listen`.
- Test refactor: Removed all Red shims so tests directly import production. Replaced regex checks over random Base64 material with literal checks after a reproduced flaky failure. A follow-up Red exposed inherited `autoIndex`; the final schema explicitly enables it only for this security-critical model.
- Focused result: GREEN — 31/31, plus ten consecutive crypto-file passes after the randomness-test correction.
- Regression result: GREEN — complete backend 54/54, complete frontend 3/3, and frontend production build pass.
- Senior implementation review: Architecture remains separated into crypto, model, service policy, and startup responsibilities. The implementation uses native crypto and existing Mongoose only; adds no repository abstraction, transaction, migration framework, rotation service, route, token behavior, or unrelated refactor. Key and decryption errors expose stable sanitized codes/messages; raw Client Secret never enters the update, safe result, JSON representation, or test diagnostics. Complete `findOneAndUpdate` plus failure injection protects the prior durable state. The additive collection can be rolled back by removing its usage without deleting stored data; real MongoDB index/envelope inspection remains explicitly deferred to `T-011` rather than mislabeled as controlled evidence.

### `T-004` authenticated configuration HTTP contracts

- Red tests: Supertest contracts require the production route to be mounted behind existing JWT authentication; controlled router tests require explicit no-record/configured/read-failure states, two-user server-derived identity, request and response whitelists, safe configured-secret indication, authoritative `credential-replacement`/`urn-only` output, actionable field errors, classified conflict/encryption/persistence responses, safe unknown-failure handling, and diagnostic metadata without controlled secret/ciphertext markers. Configuration-service cases require first save, any non-empty secret, and unchanged-ID blank-secret classification.
- Intended failure observed: The production and controlled routes return `404`, while service saves lack `changeType`. Nineteen new assertions fail for those missing behaviors; six established T-003 failure-policy tests remain Green and the independent smoke runner passes.
- Senior test review: The suite protects observable HTTP status/body/authentication/ownership behavior and the durable-save classification contract rather than controller structure. Exact response equality proves that user id, timestamps, secret value/envelope, unexpected request fields, false configured state, and false `changeType` are absent. Controlled failures include sensitive markers specifically to prove they reach neither responses nor injected logs. Missing and invalid JWT cases preserve the existing authentication contract rather than redefining it.
- Red review correction: Added the unclassified-error partition and required safe operation/code metadata so unexpected failures remain visible and diagnosable without logging raw messages, bodies, Client Secrets, or envelope fields.
- Approval: The user's standing instruction authorizes minimum Green after this senior review. No unresolved T-004 decision remains.
- Green implementation: Mounted an `/api/aps` router behind the existing JWT middleware, added narrow GET/PUT controller handlers, composed the approved configuration service with lazy environment-key encryption, and made the persistence service return its authoritative save classification only after a successful atomic write. The controller whitelists request fields and safe response fields, distinguishes absent records from read failure, maps validation/conflict/unavailable/internal failures to the approved status families, and logs only operation, public code, sanitized error classification, and stack frames without raw messages or request data.
- Green review corrections: Missing/non-string Model URN now uses the stable field-validation code; a provided non-text Client Secret is rejected rather than interpreted as blank; the URN-only HTTP test submits an actually blank secret; and error lookup requires an own property so values such as `toString` cannot hang or escape the internal-error fallback.
- Focused result: GREEN — 49/49 across canonical URN, configuration service, and HTTP contracts.
- Regression result: GREEN — complete backend 74/74, complete frontend 3/3, and frontend production build pass.
- Senior implementation review: The API reuses current auth and Express composition, keeps HTTP mapping outside persistence/domain code, and introduces no global error framework or auth response change. Production key access is lazy, so reads and unrelated startup remain available when encryption configuration is unavailable, while saves fail closed before persistence. Service-derived `changeType` cannot be returned before durable success. Responses and diagnostics exclude user id, timestamps, Client Secret, envelope fields, request extras, and raw causes. Rollback removes only the APS router/controller mounting and save-classification response while retaining the additive encrypted data boundary.

### `T-005` APS token exchange and Viewer callback provider

- Backend Red tests: Controlled configuration, encryption, fetch, timer, response, router, and logger boundaries require current-user secret selection, exact OAuth v2 endpoint, HTTP Basic credentials, form body with only `viewables:read`, `no-store`, one request, ten-second abort/cleanup, positive integer lifetime, camel-case safe result, and classified missing/rejected/invalid/temporary/internal outcomes without reading rejected bodies or exposing raw markers.
- Frontend Red tests: A pure provider contract requires `getAccessToken(onTokenReady)` to make an argument-free authenticated API request, deliver only token/lifetime, repeat ordinary renewal on the same callback, perform no lifecycle/token-setter/storage work, invalidate older user/workspace/authentication/runtime registrations, suppress stale success/failure, notify only the current workspace with a code-only normalized error, and stop after release or logout-style clear.
- Intended failures observed: Backend reports 23 missing-behavior failures plus one existing auth guard pass. After correcting the test-only missing-module shim, frontend reports 12/12 behavior failures with no unhandled rejection.
- Senior test review: Network tests assert only public fetch inputs/outputs and controlled failures; they do not call APS or preserve a token. Provider tests use promises and callback spies rather than Viewer/WebGL mocks. The four context dimensions intentionally protect the generation ownership contract, while release and clear separately protect no-active-context and logout. Exact negative assertions prohibit browser-supplied credentials reaching the token service, raw upstream/error material reaching responses/logs/notifications, and token writes reaching browser storage.
- Red review corrections: Replaced the Vite-incompatible file-URL shim and then replaced its no-op false positives with a promise-consuming deliberately wrong provider. Added argument-free token-request proof and Axios-shaped code normalization. The reviewed Red is deterministic and behavior-driven.
- Approval: The user's standing instruction authorizes minimum Green after this senior review. No unresolved T-005 decision remains.
- Green implementation: Added an injected-fetch token service that reads/decrypts only current-user configuration, sends exactly one Basic-authenticated form request to the approved OAuth v2 endpoint with only `viewables:read`, disables caching, aborts after ten seconds, validates token/lifetime, and returns only the camel-case callback payload. Added authenticated POST routing with safe status/message mapping and diagnostics. Added an Axios token request with no body and a pure registration-scoped callback provider that captures user/workspace/authentication/runtime context, renews through repeated Viewer callback calls, normalizes errors to code only, and retains no token.
- Green review corrections: Kept the abort timer active through body parsing, classified body-read abort as temporary, rechecked current registration before starting the request microtask, captured the registration's original notifier, and proved token-controller response whitelisting even when a controlled service returns extra secret fields.
- Focused result: GREEN — backend 26/26 and frontend 13/13.
- Regression result: GREEN — complete backend 100/100, complete frontend 16/16, and frontend production build pass.
- Senior implementation review: Backend configuration, encryption, OAuth transport, and HTTP mapping remain separate and use only native runtime APIs/existing dependencies. The flow has no retry, refresh token, token cache, token persistence, raw upstream-body read on rejection, or browser credential input. Frontend state retains only registration identity/context and callbacks; token values exist only in promise scope until `onTokenReady`. Ordinary renewal calls the same provider without lifecycle operations. New registration, release, and clear suppress obsolete requests/results/errors. Real APS authorization and Viewer-managed renewal are deliberately not claimed and remain required live evidence in `T-011`. Rollback removes the token handler/service/provider/API without altering stored configuration.

### `T-006` frontend settings and authoritative commit-point state

- Red tests: The API adapter must use the existing authenticated Axios client for exact GET/PUT contracts. The controller must distinguish loading, explicit empty, configured, read error, saving, and save error; retain attempted versus committed values; accept only backend-authoritative lifecycle classification; advance only the branch-specific generations; suppress stale read/save completions; prevent concurrent saves; and emit no lifecycle command for empty, read error, failed save, or invalid success classification. React interaction tests require labeled required fields, password/autofill semantics, configured-secret indication without readback, accessible loading/error/retry/success feedback, disabled submission, field association, and secret clearing only after durable success.
- Intended failure observed: All 19 tests execute through deliberately incapable absent-subject fallbacks and fail on missing behavior rather than module resolution. The independent frontend smoke test remains Green.
- Senior test review: The suite traces to the T-006 Red evidence and the relevant configuration, atomicity, accessibility, generation, and lifecycle criteria. API assertions stay at the public Axios boundary; controller assertions protect observable state and emitted commands rather than reducer internals; component assertions use roles, labels, values, and user interaction rather than class names or snapshots. First save, unchanged-ID blank-secret replacement, non-empty-secret rotation, changed-ID replacement, durable failure, unknown classification, active-operation secret clearing, newer form edits, user/workspace change, logout-style clearing, explicit absence, and read failure are separate realistic partitions.
- Red review corrections: Replaced DOM matchers unavailable in the approved toolchain with native DOM properties, added explicit first-save generation/command proof, required newer non-secret edits as well as secret edits to survive an active response, and supplied a controlled pending read so the stale-save test cannot change phase through an unrelated exhausted mock.
- Approval: The user's standing instruction authorizes minimum Green after this review. No unresolved T-006 product or architecture decision remains.
- Green implementation: Added exact configuration GET/PUT methods over the existing authenticated Axios client; a feature-local reducer/controller for attempted versus committed state, operation/context guards, backend-authoritative save classification, branch-specific generations, and lifecycle commands; an accessible settings panel; and replacement of only the home placeholder while preserving the existing shell. The first save and credential replacement clear only the submitted secret after durable success. Empty/read-error/failed/stale operations start no model lifecycle work.
- Green review corrections: Explicitly mapped only Client ID, canonical Model URN, and configured-secret indication from both read/save responses before state or command publication. Added reducer atomicity/clear invariants and home integration evidence. Guarded the pre-existing token-without-user storage condition so the shell remains usable, gives corrective sign-in guidance, and makes no APS call. Corrected conditional ARIA description references and replaced all Red fallbacks with direct production imports.
- Focused result: GREEN — 24/24 across API, reducer, controller, and React/home tests.
- Regression result: GREEN — backend 100/100, frontend 40/40, and frontend production build pass.
- Senior implementation review: The increment follows existing Axios/AuthContext/Tailwind patterns and adds no dependency or global state redesign. State ownership is feature-local; the reducer is pure; asynchronous guards are isolated in the controller; presentation uses labels, password semantics, keyboard-native form controls, `aria-live` feedback, field associations, and no blocking alert. Only an actively attempted Client Secret exists in React state, and it survives failure/newer edits but clears after its matching durable success. Unknown success classification fails closed, backend errors retain actionable safe messages, and obsolete work is intentionally silent. Lifecycle commands expose only safe committed configuration and generations; actual Viewer ownership remains correctly deferred to `T-007`. Rollback removes the settings feature/home replacement while leaving backend persistence and token APIs intact.

### `T-007` serialized Viewer lifecycle and deterministic 3D loading

- Red tests: Asset cases require exact matching `7.118.2` script/CSS URLs, one shared in-flight promise, preloaded-runtime reuse, safe failure cleanup, and retry. Pure selection cases require a supported default 3D geometry or first supported public-child DFS result and reject 2D/non-geometry/no-3D outcomes. Controlled lifecycle cases require exact Initializer options/token context, one Viewer/model, URN-only reuse, credential-reset `finish()` then `shutdown()` then reinitialize order, first-save behavior, document/asset/no-3D/start/init failure feedback, retry, partial cleanup, overlapping reset coalescing, stale document/model/token suppression, idempotent disposal, absent-host safety, and teardown-before-remount serialization.
- Intended failure observed: All 28 cases fail through deliberately incapable absent-subject fallbacks. The runner, existing fixture imports, promises, coordinator/SDK doubles, and jsdom remain operational; the independent smoke test passes.
- Senior test review: The suite models only consumed public methods and callbacks (`Initializer`, `GuiViewer3D`, `Document.load`, `loadDocumentNode`, `unloadModel`, `finish`, `shutdown`, BubbleNode predicates/children, and token registration). It asserts observable calls, order, ownership counts, state, and safe feedback without testing Autodesk implementation details, rendering, WebGL, or private fields. Initial load, URN-only replacement, credential reset, ordinary retry, reset retry, final teardown, remount, overlapping commands, and stale completion are distinct partitions.
- Red review corrections: Added preconditions before accessing absent fallback artifacts so missing behavior produces assertions rather than harness `TypeError`; made deferred boundaries wait for the intended public call; added partial Viewer-start cleanup, post-reset retry without duplicate shutdown, stale loaded-model unloading, no-3D/no-2D behavior, loader retry, current-versus-obsolete credential feedback, and a React-owned host/disposal/retry boundary. Controlled evidence remains explicitly non-live.
- Approval: The user's standing instruction authorizes minimum Green after this review. No unresolved T-007 decision remains.
- Green implementation: Added one exact `7.118.2` JavaScript/CSS loader, one pure public BubbleNode selector, one serialized lifecycle coordinator, and one React-owned host. The coordinator initializes through the supported callback, owns at most one active Viewer/model, reuses a healthy runtime for URN-only replacement, performs `finish()` then public `shutdown()` before credential reinitialization, serializes commands/disposal, and suppresses stale document/model/token work by captured operation/runtime context.
- Green review corrections: Removed all absent-subject test fallbacks in favor of direct production imports. A new Red proved that failed shutdown ownership was discarded; the corrected coordinator retains only the pending runtime reference and retries teardown before reinitialization without repeating Viewer finish. Another Red proved stylesheet failure was silent; matching CSS and JavaScript now form one listener-cleaned load operation with actionable failure and retry. A final accessibility Red added explicit alert semantics, and its test was corrected to use the repository's native DOM assertion surface.
- Focused result: GREEN — 30/30 across asset/selection, lifecycle, and React host files.
- Regression result: GREEN — backend 100/100, frontend 69/69, and frontend production build pass with 103 transformed modules.
- Senior implementation review: The implementation uses only the approved Viewer `7.118.2` distribution and public `Initializer`, `GuiViewer3D`, `Document.load`, `loadDocumentNode`, `unloadModel`, `finish()`, `shutdown()`, and BubbleNode methods. Asset, selection, coordination, token, settings, and presentation responsibilities remain separated without a Viewer wrapper dependency or unrelated refactor. Secrets and tokens are neither persisted nor logged; captured commands whitelist safe configuration; raw SDK/upstream details do not reach feedback. Retry preserves a healthy runtime for model failures, repeats teardown before reinitialization only when shutdown ownership remains uncertain, and leaves real WebGL/APS behavior explicitly open. Rollback removes the four Viewer modules and home wiring while retaining the configuration/token boundaries.

### `T-008` instance/category resolution and native toolbar theming

- Red tests: Pure resolver tables require only the eight approved trimmed case-insensitive aliases, reject partial/fuzzy/localized/inferred values, deduplicate proven instance `dbId` values, include a proven non-leaf instance, exclude explicit non-instance records and a terminal record without instance proof, and fail only an affected category when evidence is unknown or contradictory. Adapter cases require public tree traversal, direct-fragment evidence, callback deduplication, bounded `getBulkProperties2` reads with only visible Category data, stable failure mapping, and conservative failure when required classification APIs are absent. Toolbar cases require one delayed-or-immediate native group, three labeled keyboard controls, accessible unavailable/pressed state, distinct active-model theming, overlap-safe toggle-off rebuild, separate zero-match/failure feedback, independent readiness, feature-only reset, listener/group cleanup, and inert stale controls.
- Intended failure observed: All 31 cases execute through deliberately incapable absent-subject fallbacks and fail on missing category, adapter, or toolbar behavior. The runner, controlled DOM/UI/model doubles, and asynchronous callbacks remain operational; the independent smoke test passes.
- Senior test review: Category tables trace directly to the eight approved aliases and instance-identity exclusions without defining a universal Revit ontology. The adapter double models only supported tree, fragment-enumeration, and `getBulkProperties2` callbacks, and proves bounded request shapes rather than Viewer internals. Toolbar tests assert public calls, ownership counts, model argument, accessible semantics, feedback, and theming outcomes rather than SDK rendering or classes. Zero-match, category failure, active overlap, replacement-style reset, delayed toolbar creation, repeated mount/event, and final disposal are distinct partitions.
- Red review corrections: Added actual duplicate tree callbacks to the deduplication case; stopped trimming the Category property name because only its values have approved normalization; added preconditions so absent toolbar behavior fails by ownership assertion rather than `TypeError`; replaced a translated fixture literal with English-only unsupported input; and required `aria-disabled` plus `aria-pressed` so control meaning does not depend on color.
- Approval: The user's standing instruction authorizes minimum Green after this reviewed Red. No unresolved T-009 decision remains.
- Green implementation: Added a public instance-tree/property adapter, exact category resolver, result-only model analysis experience, one native toolbar controller, lifecycle-coordinator integration, distinct active-model colors, and accessible partial-failure retry. The adapter recognizes exact Revit category containers before geometry, walks through organizational levels, accepts the first renderable descendant on each category branch, stops before nested geometry, and requires its exact Category property to agree with the structural category.
- Senior implementation review: Independent review exposed and corrected stale toolbar mutation before generation guards, disconnected adapter/resolver evidence, unsafe ancestry inference, malformed-success-to-zero conversion, theming cleanup aborts, swallowed safe diagnostics, and category errors with no usable retry action. Tests now keep orchestration result-only, apply toolbar mutations only after active-operation checks, fail ambiguous or malformed evidence conservatively, guarantee best-effort state/ownership cleanup, and log only stable stage/category codes.
- Evidence boundary: The supplied Revit screenshots support the shallow category-container structure but do not prove returned API fields or native SDK behavior. Final representative model/property, real native toolbar/theming, rendering, and WebGL evidence remains explicitly assigned to `T-011`.
- Final result: GREEN — 69/69 focused controlled tests; 119/119 complete frontend tests; 100/100 backend tests; production build passes with 107 transformed modules.
- Representative-model follow-up: User screenshots exposed counted category labels, aggregate-fragment containers, empty category controls, and non-semantic initials. Reviewed Red/Green corrections preserve exact property matching, reach first renderable descendants through the real Revit category structure, and use three scoped Material Symbols. The updated complete frontend suite passes 120/120; actual color rendering remains a live `T-011` proof rather than an automated claim.
- Live-model correction: Subsequent controlled inspection supersedes the property-confirmed candidate strategy. The representative derivative exposes supported category nodes directly below the root and no element property named `Category`; the user explicitly approved exact root-category matching with recursive leaf descendants. Reviewed Red/Green coverage now protects that public-tree contract, and live Viewer evidence confirms Furniture, Walls, and Doors theming plus pointer toggle-off state.

### `T-009` quantity domain and progressive Area orchestration

- Red tests: Pure tables require deduplicated leaf counts, full-string finite nonnegative decimal parsing, exact trimmed case-insensitive Area names, proven duplicate identity, trim-only units, incompatible-unit rejection, complete/partial/unavailable states, zero, and finite totals. Adapter/controller tests require bounded visible-property reads, public-field whitelisting, safe operational failures, count-before-Area, zero skip, failed-count skip, category isolation, abort between batches, stale success/error suppression, and idempotent disposal/delivery.
- Intended failure observed: All 66 assertions failed only because the three production subjects were absent; the test runner and controlled doubles remained functional. One deferred rejection and missing callback/signal access were corrected in the harness before Green.
- Approval: The user's standing instruction authorizes minimum Green after senior review. No unresolved T-009 product decision remains.
- Green implementation: Added a pure numeric/report domain, bounded `getBulkProperties2` adapter with visible properties and an allowlisted result shape, and a model-local Door/Window quantity controller. The controller consumes only T-008 leaf ids, publishes safe count before Area, isolates category failures, retains count on operational Area failure, aborts stale work between batches, and publishes no stale result/error.
- Senior review corrections: Removed the restrictive Area property filter so valid casing/whitespace reaches the domain matcher; added negative infinity/string, ambiguous-partial, unitless/unit-bearing duplicate, exact no-subtotal, stale rejection, and overflow evidence. Finite contributions that overflow during aggregation now produce `unavailable` rather than an unsafe infinite total.
- Result: GREEN — 68/68 focused tests; 193/193 complete frontend tests; production build passes with 107 transformed modules. Real property shapes/duplicate metadata and accessible report presentation remain explicitly assigned to `T-011` and `T-010` respectively.

### `T-010` accessible workspace and automated convergence

- Red tests: Integrated report, Viewer host, lifecycle, and home-shell scenarios require seven honest quantity states, semantic headings/regions, nonduplicated live feedback, Viewer preservation, actionable retry, sanitized Door/Window-only publication, progressive state, stale suppression, and ordered disposal.
- Intended failure observed: 12 new assertions failed because quantity results were not yet composed into the workspace; 32 existing assertions stayed Green. A test-harness factory default was corrected before production work.
- Approval: The user's standing instruction authorizes minimum Green after senior review. No unresolved T-010 product decision remains.
- Green implementation: Added a feature-local accessible report, connected the quantity experience through the lifecycle coordinator, and retained the ready Viewer while count and Area results progress or fail independently. Only safe canonical Door/Window results reach React state.
- Senior review corrections: Added spacing between labels and values, caught synchronous quantity-controller failures, separated assertive errors from polite live regions, converted malformed current-model output to a conservative terminal failure, and proved quantity disposal precedes toolbar/runtime teardown. Obsolete malformed output remains silent.
- Result: GREEN — 46/46 focused tests; 206/206 complete frontend tests; production build passes with 111 transformed modules. Live representative-model composition also passes with Door count 11 and Window count 17; both Area results honestly remain unavailable pending T-011 property-shape analysis.

### `T-010A` native quantity extension, element navigation, and panel UX

- Red evidence: Controlled extension expectations initially failed for the missing artificial-root suppression, resizable dimensions, category/item navigation, and close-time isolation cleanup. The failures were behavior-specific; existing toolbar and quantity-state cases remained executable.
- Green implementation: Replaced the page report with `Aps.ModelQuantities`, one separate native toolbar control, bounded resizable `DockingPanel`, and one native `Tree`/`TreeDelegate`. The structural root is hidden; Door and Window groups use aligned rotating indicators, multiline summaries, indented element rows, user-facing Autodesk Area units, and exactly two decimal places for aggregate display. Sanitized quantity rows remain id-free while a separate lifecycle channel supplies current-model ids solely for public `isolate`/`fitToView` navigation. Closing/resetting clears panel-owned isolation with `isolate([])` and never clears theming.
- TDD review: The controlled Viewer double covers observable ownership, accessibility, summary/detail states, hierarchy, navigation calls, cleanup, stale suppression, safe failures, and absence of displayed ids without simulating WebGL. Aggregate/detail behavior shares the existing contribution resolver. Real Viewer validation remains distinct from controlled tests.
- Automated result: PASS — 39/39 focused extension/lifecycle tests; 222/222 complete frontend tests across 18 files; 100/100 backend tests; Vite production build passes with 111 transformed modules.
- Live APS result: PASS for the representative translated Revit model — one separate command opens the native panel; the artificial root is absent; Doors and Windows show counts 11 and 17 and totals `21.20 m²` and `63.37 m²`; expansion exposes ordered element rows; category activation isolates and fits; closing with `X` hides the panel, clears isolation, returns the command to inactive, and preserves the already-applied red category theming. The resize affordance and bounded CSS were visually present, but an automated corner drag was obstructed by the Viewer toolbar overlay and is not claimed as completed manual resize evidence.
- Security/error review: Neither rendered text nor feedback contains `dbId`, raw properties, APS credentials, tokens, URNs, or raw SDK failures. Navigation failure uses one actionable English message. No dependency, storage, private Viewer API, or theming ownership was added.
- Rollback: Remove the extension and its lifecycle id/result wiring, restore the prior page report, and leave secure configuration, token exchange, model loading, category resolution, theming, and quantity computation unchanged.

### `T-011` partial live APS and local-security validation

- Live runtime and derivative: PASS for matching Viewer JS/CSS paths pinned to `7.118.2`, one usable 3D Viewer region, real derivative rendering, native controls, and no captured Viewer/application console error. Only the base application's existing React Router future-flag warnings were present.
- URN-only save: PASS with the currently configured canonical URN and blank secret field. The authenticated save succeeded, the 3D model returned ready, and exactly one Viewer region and one quantity command remained. Controlled lifecycle tests remain the evidence for internal no-reinitialization ordering because live instrumentation did not alter or wrap Viewer runtime functions.
- Revit tree and quantities: PASS for direct-root category behavior without an element `Category` property dependency, recursive leaf results, 11 Door rows, 17 Window rows, official square-meter label mapping, total displays `21.20 m²` and `63.37 m²`, ordered individual rows, hidden structural root, and no displayed `dbId`/raw property record.
- Native interaction: PASS for pointer and keyboard expansion, `aria-expanded`, element availability, category isolate-and-fit, toolbar/panel active-state synchronization, close-time isolation cleanup, and independent Furniture/Wall theming (both active together; turning Furniture off retained Walls; original states restored). Existing theming remained after quantity-panel close in the dedicated live check.
- MongoDB read-only inspection: PASS for one current configuration, one unique `userId` index, unique per-user documents, canonical stored URN, a versioned encrypted envelope with ciphertext/IV/authentication tag, and absence of a plaintext `clientSecret` field. No stored value was printed.
- Storage and audit: Static source inspection confirms APS access tokens remain transient; the base application intentionally retains only its existing application JWT/user session in `localStorage`. Live browser storage values and cookies were not inspected. `npm audit --omit=dev` reports zero backend vulnerabilities and two moderate frontend React Router advisories; no unrelated dependency remediation was performed.
- Accepted live-validation limitations: observable ordinary APS token renewal; alternate valid changed-client/non-empty-secret replacement and old-runtime silence; reset-failure injection; live fallback 3D selection and explicit no-2D fallback; full Chrome/Edge matrix; live network-header/body and browser-storage inspection. These are documented in `ASSESSMENT.md` and are not inferred from controlled tests. The project owner separately confirmed manual acceptance of the implemented Viewer flow, including the current panel behavior.

### Final assessment documentation

- Project-owner decision: Use the assessment's permitted separate-document option. Root `ASSESSMENT.md` is the single complete English guide; README receives only one separated final reference.
- Coverage: The guide documents environment setup and key generation, APS/model configuration, category colors, native quantities, environment/dependency changes, architecture/security decisions, AI-assisted SDD/TDD process, validation, limitations, and troubleshooting.
- README preservation: `git diff -- README.md` shows six appended lines and zero removed or modified tracked lines. Existing README content remains unchanged.
- Security review: The guide contains placeholders and generation commands only. No real Client ID, Client Secret, encryption key, JWT, APS token, Model URN, environment file, database value, or raw property record is included.

## Acceptance traceability

| Acceptance criterion | Evidence | Status | Gap |
| --- | --- | --- | --- |
| `AC-028` | One project-local skill plus structural and pressure-scenario validation. | Pass for `T-001` | Later implementation must continue to follow it. |
| `AC-029` | Focused runners and controlled fake-key, API, model/property, bubble-tree, toolbar, Viewer, and async-race fixtures/doubles. | Pass for `T-001` | Behavioral suites arrive in their traced tasks; live proof remains open. |
| `AC-030` | English-content and diff review of all feature-owned material introduced by `T-001`. | Pass for `T-001` | Future feature increments require their own review. |
| `AC-031` | Backend accepted-input table and frontend exact-identifier table, followed by focused and full Green results. | Pass for `T-002` | Persistence and automatic load evidence arrive in their dependent tasks. |
| `AC-032` | Backend rejected-input table covers every approved syntax partition and both unused-bit boundaries. | Pass for `T-002` | Field-specific UI guidance arrives in `T-004` and `T-006`. |
| `AC-003`-`AC-007` | Controlled crypto/model/service tests cover canonical per-user state, encrypted envelope, blank-secret retention, replacement-secret requirement, safe projections, atomic save, and failure rollback. | Partial through `T-003` | Authenticated HTTP, frontend commit-point behavior, and real MongoDB inspection remain in `T-004`, `T-006`, and `T-011`. |
| `AC-029` | Focused T-003 tests use synthetic keys and controlled Mongoose/startup doubles for security-sensitive deterministic behavior. | Pass for T-003 scope | Real MongoDB and APS evidence remains open and is not represented as automated or live. |
| `AC-033`, `AC-035`, `AC-036` | Server-derived service filters, safe read failures, validation/encryption/write failure injection, and exact prior-state assertions. | Partial through `T-003` | HTTP error mapping, UI retry, and Viewer non-regression behavior arrive in dependent tasks. |
| `AC-001`-`AC-007` | Authenticated GET/PUT contracts, server-derived identities, explicit absent/configured/read-error states, safe response mapping, secret rules, and authoritative post-write classification. | Pass for backend HTTP scope through `T-004` | Frontend settings, commit-point state, and live Mongo inspection remain in `T-006` and `T-011`. |
| `AC-024`, `AC-035`, `AC-036` | Actionable English API messages, field errors, retry guidance, no false save/read success, and sanitized diagnostic assertions. | Partial through `T-004` | Accessible rendering and Viewer-state preservation remain in frontend/lifecycle tasks. |
| `AC-008`, `AC-009`, `AC-033` | Exact current-user 2LO exchange, least scope, validated token/lifetime response, no browser credentials, and no storage/log/cache token path. | Pass for controlled T-005 scope | Real APS credentials and browser network/runtime inspection remain in `T-011`. |
| `AC-021`, `AC-023`, `AC-045`, `AC-054` | Registration-scoped callback renewal, four context dimensions, release/clear, stale success/error suppression, and no lifecycle/token-setter methods. | Partial through `T-005` | Actual Initializer/runtime ownership, shutdown rules, and live renewal remain in `T-007` and `T-011`. |
| `AC-001`-`AC-006`, `AC-031`, `AC-032`, `AC-035`, `AC-036` | Authenticated home settings, explicit empty/configured/read-error states, first/update secret semantics, field guidance, durable commit-point state, retry, and exact failed-save preservation. | Pass for automated T-006 scope | Real Mongo inspection and browser/manual behavior remain in `T-011`. |
| `AC-021`, `AC-022`, `AC-046`, `AC-050`-`AC-052`, `AC-056` | User/workspace/save guards, monotonic branch generations, initial/model-replacement/reset commands, no command on failed/invalid/stale operations, and safe committed-response whitelisting. | Partial through `T-006` | Viewer execution, stale Viewer work, failure recovery, and public lifecycle ordering remain in `T-007`/`T-011`. |
| `AC-024`, `AC-029`, `AC-030`, `AC-037`-`AC-039` | English accessible labels/password semantics/status/error/retry/field associations, controller doubles, home-shell integration, safe error messages, and no raw response/state leakage. | Partial through `T-006` | Integrated Viewer failures, keyboard/browser manual checks, and live APS behavior remain open. |
| `AC-008`, `AC-010`, `AC-021`-`AC-023`, `AC-036`-`AC-041`, `AC-045`, `AC-046`, `AC-050`-`AC-056` | Exact assets/options, canonical document id, deterministic public 3D selection, serialized runtime/model ownership, both replacement branches, stale suppression, cleanup/retry, safe feedback, and React host ownership. | Pass for controlled `T-007` scope | Real APS token renewal, derivative access, WebGL, runtime version, public shutdown, and model document evidence remain in `T-011`. |
| `AC-011`-`AC-016`, `AC-021`-`AC-024`, `AC-029`, `AC-042` | Exact direct-root category aliases, recursive leaf collection, callback/model-local deduplication, independent native controls, active-model overlap-safe theming, pointer/keyboard state, stale suppression, accessible partial failures, retry, cleanup, and integrated report presentation. | Pass through controlled `T-010`; live theming and report composition pass for the representative model | Remaining lifecycle/browser acceptance stays in `T-011`. |
| `AC-011`-`AC-013`, `AC-017`-`AC-021`, `AC-029`, `AC-034`, `AC-043`, `AC-044`, `AC-048`, `AC-049` | Leaf-derived counts, exact Area parsing, duplicate proof, unit compatibility, report states, bounded public-property adaptation, progressive publication, isolated operational failure, stale batch/result/error suppression, and accessible Door/Window rendering. | Pass through controlled `T-010A`; live counts, Autodesk square-meter mapping, individual values, and totals pass for the representative model | Additional real duplicate/incompatible metadata shapes remain in `T-011`. |
| `AC-057`-`AC-060` | Controlled extension ownership, native tree states, safe element detail, resize constraints, row navigation, cleanup, stale suppression, live Viewer panel/expansion/isolation/close/theming evidence, and project-owner manual acceptance. | Pass through `T-011` | Full automated cross-browser WebGL coverage is an accepted documented limitation. |
| `AC-027`, `AC-030`, `AC-047` | Complete English root `ASSESSMENT.md`, append-only README reference, English local commit history, secret scan, and generated/dependency/environment exclusions. | Pass for documentation and local delivery scope through `T-011` | Branch/fork publication remains separately authorized in `T-012`. |

## Senior convergence review

- Correctness and regressions: Through `T-010A`, completed backend evidence, the complete frontend suite, and the frontend production build pass. Canonical input, encrypted atomic persistence, safe configuration/token APIs, transient callback tokens, durable settings commit points, serialized Viewer lifecycle, model-local category resolution, native-control ownership, theming, deterministic quantity behavior, and native quantity-panel composition remain covered at their appropriate boundaries.
- Security and secret handling: Fixtures use synthetic values only. Backend projections and frontend response mappers exclude secret/token/envelope extras; the Client Secret remains only in active form state until its matching save succeeds; token/provider work writes no browser storage. Existing audit findings remain recorded without unrelated automatic remediation.
- Accessibility and user feedback: T-006 provides associated English labels, password semantics, native keyboard submission/retry, field errors, status/error live regions, configured-secret guidance, and a shell-safe missing-account path. T-007 adds an accessible Viewer region, explicit failure alert, actionable messages, and keyboard-native retry. T-010A adds a labeled native command, keyboard-operable group/item rows, multiline hierarchy, aligned non-color-only indicators, safe progressive states, and actionable navigation failure while preserving the Viewer. Live keyboard behavior and project-owner manual acceptance pass; a full cross-browser matrix remains a documented limitation.
- Maintainability and complexity: Each implemented responsibility remains feature-local and uses existing dependencies/patterns. No auth, Axios, shared-shell, global-state, design-system, or unrelated refactor was introduced.
- Operations and recovery: Completed increments have independent rollback boundaries. T-007 can remove Viewer modules/home wiring without changing stored configuration or token APIs; healthy model retries reuse the runtime, uncertain shutdown ownership blocks reinitialization until teardown succeeds, and obsolete operations publish nothing. T-009 can remove its domain/adapter/controller without affecting model loading or category theming. T-010A can remove native panel composition and quantity lifecycle wiring without changing secure persistence, token exchange, category theming, or quantity computation.
- Documentation synchronization: Approved Specification, Technical Plan, and Tasks contain the user-approved category, native-panel, interaction, and separate assessment-guide amendments. `ASSESSMENT.md`, the appended README reference, architecture, tasks, and validation describe the completed behavior and distinguish live evidence from accepted limitations.
- Diff and Git hygiene: Increment reviews preserve every existing README line and add only the approved guide reference, exclude real secrets/tokens/generated output, and separate tests, production behavior, and documentation into reversible English commits.

## Limitations and proof gaps

- Controlled fixtures and smoke tests do not prove Autodesk rendering or WebGL by themselves; representative-model live evidence separately proves the implemented Viewer, toolbar, category, Area, and quantity-panel path.
- No live APS credential, token, URN, or model data is stored in the repository or used by completed automated increments.
- Live root-category, native-theming, pointer/keyboard toggle, count, Area mapping, quantity-panel layout, row navigation, close cleanup, MongoDB security inspection, and project-owner manual acceptance are complete. Alternate credential replacement/renewal observation, injected reset failure, fallback 3D selection, live network/storage inspection, and a full cross-browser matrix remain accepted limitations documented in `ASSESSMENT.md`.
- The repository's existing `nodemon`, `postcss`, and React Router dependency paths retain the audit findings recorded above. Updating them is not required for test preparation and would be unrelated dependency work.

## Final decision

- [x] Approved requirements, plan, tasks, tests, code, and documentation converge.
- [x] Evidence classifications are honest.
- [x] No material finding remains unresolved.
- [ ] User accepted Convergence and authorized the requested Delivery action.
