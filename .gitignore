
# Ignore cursor AI rules
.cursor\rules\codacy.mdc

# Node
node_modules/
.env

# Infrastructure — commit volume data so the DB state travels with the repo
# (intentionally NOT ignoring infrastructure/mongo-data/)

